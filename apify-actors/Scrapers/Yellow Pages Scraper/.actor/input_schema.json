{
  "title": "Yellow Pages Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQueries": {
      "title": "🔍 Search Terms",
      "type": "array",
      "description": "Enter business types, one per line (e.g., 'plumbers', 'restaurants', 'lawyers').",
      "editor": "stringList",
      "sectionCaption": "1. Search Configuration",
      "items": { "type": "string" }
    },
    "locations": {
      "title": "📍 Locations",
      "type": "array",
      "description": "Enter locations, one per line (e.g., 'New York, NY', '90210', 'San Francisco, CA').",
      "editor": "stringList",
      "items": { "type": "string" }
    },
    "maxTotalBusinesses": {
      "title": "Limit: Total Businesses to Scrape",
      "type": "integer",
      "description": "Controls the total number of unique business pages the Actor will scrape.",
      "default": 10
    },
    "maxBusinessesPerSearchPage": {
      "title": "Businesses per Search Page",
      "type": "integer",
      "description": "The maximum number of business links to pull from *each* search results page.",
      "default": 5
    },
    "verboseLog": {
      "title": "Verbose Logging (Debug)",
      "type": "boolean",
      "description": "Enables detailed logging for debugging.",
      "default": false
    },
    "proxyConfiguration": {
      "title": "⚙️ Proxy Configuration",
      "type": "object",
      "description": "YellowPages.com requires a US-based proxy to work. Please use Residential Proxies set to the 'United States'.",
      "editor": "proxy",
      "sectionCaption": "2. Technical Configuration"
    }
  },
  "required": [
    "searchQueries",
    "locations",
    "maxTotalBusinesses",
    "maxBusinessesPerSearchPage"
  ]
}