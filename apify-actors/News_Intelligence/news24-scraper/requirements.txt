# Core Apify actor runtime
apify==1.7.2

# Playwright for async browser automation
playwright==1.49.0

# Optional helper for bot-detection evasion
playwright-stealth==1.0.6

# HTML parsing and fallback extraction (optional but useful)
beautifulsoup4==4.12.3
lxml==5.3.0

# Logging and data utils
aiofiles==24.1.0
tenacity==9.0.0  # retry logic, optional